var compteur = function(texte) {
  return ((texte.split(" ")).length);
}
var extrait = "salut tu vas bien";
console.log(compteur(extrait));

// essai 2
var extrait = "salut tu vas bien";
var split = function(texte) {
  return ((texte.split(" "))):
}
console.log(extrait);

function compteur2 = function(table) {
  for (var i=0; i<table.length; i++) {
    if (table[i]).length <= 2) {
      table.slice (i,1);
    }
  }
}
